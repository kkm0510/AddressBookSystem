#Features Added ->

* All inputs(console, csv, json) are checked via regex before loading and skipped if wrong.

* Repeated names in same book are skipped while reading data from console/csv/json.

* All invalid data along with reason for being invalid is collected and feature to store it in a separate file is provided.